#include <stdio.h>
#include <stdio.h>

int number, unit,rev_num=0; 
int main()
{
	printf("Enter the number: ");
	scanf("%d",&number);
	
	while(number>0)
	{
	    unit = number%10;
	    rev_num = rev_num * 10 + unit; 
	    number /= 10;
	}
	
	printf("The reversed number is: %d.", rev_num);
	
}
